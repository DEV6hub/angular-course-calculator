'use strict';

describe('Directive: calculatorButton', function () {

  // load the directive's module
  beforeEach(module('webCalculatorApp'));

  /*var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<calculator-button></calculator-button>');
    element = $compile(element)(scope);
    expect(element.text()).toBe('this is the calculatorButton directive');
  }));*/
});
